Delimiter-rich reference
========================

.. _target--name:

Double-hyphen target
--------------------

The source docname and label spelling both contain ``--``. Sphinx normalizes
the label's generated target ID to ``target-name``.
